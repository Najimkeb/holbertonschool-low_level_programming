#include "main.h"
#include <stdio.h>

/**
 * void times_table(void) - check the code
 * resultat
 * return à zero
 */

void times_table(void);
{
	int n, i;

	for (n = 9; n++)

		for (i = 1; i <= 10; ++i)
		{
			resultat = n * i;
			printf("%d\n", resultat);
		}
	return (0);
}
