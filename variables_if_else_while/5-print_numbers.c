#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int n;
for ('%d' = 0 ; '%d' <= 10 ; c++)
putchar(c);
putchar('\n');
return (0);
}
